#include "OgreSceneLoader.h"

namespace Ogre {
    
    bool SceneLoader::load(const String& filename, const String& groupname, SceneNode *rn)
    {
        return false;
    }
    bool SceneLoader::load(const String& filename, const String& groupname, SceneManager *sm)
    {
        return false;
    }
    
}
